[package]
name = "crap-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Language-agnostic CRAP scoring, LCOV join, and reporting"
readme = "../../README.md"
repository = "https://github.com/deangrant/crap-score"
keywords = ["coverage", "complexity", "metrics"]
categories = ["development-tools::testing"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints]
workspace = true
