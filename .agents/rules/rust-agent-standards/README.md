# Rust Agent Standards Rule

File-scoped constraints for Rust edits: style/SOLID skills, lint policy,
and coverage.

## When to use

Enable when working on `*.rs` files.

## Usage

Cursor loads the `.mdc` when matching files are in context.
